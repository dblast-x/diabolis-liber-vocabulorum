""" 
    Steps >>
      1. Get word as request.
      2. Open db.
      3. Search for word.
        ( if word in db. ->> return definition as response.
        (( else          ->> return error as typo.
"""
